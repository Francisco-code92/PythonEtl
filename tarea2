SELECT d.paisnombre AS pais,
       TO_CHAR(v.fecha, 'YYYY-MM') AS aniomes,
       SUM(v.netusd) AS netusd_total,
       SUM(v.taxusd) AS taxusd_total
FROM ventas v
JOIN dimpais d ON d.paiscode = v.paiscode
GROUP BY d.paisnombre, TO_CHAR(v.fecha, 'YYYY-MM')
ORDER BY d.paisnombre, aniomes;
